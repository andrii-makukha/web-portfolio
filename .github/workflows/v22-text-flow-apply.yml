name: Apply v2.2 Text Flow Patch

on:
  push:
    branches:
      - portfolio-v2.2-text-flow-stress
    paths:
      - ".github/workflows/v22-text-flow-apply.yml"
      - "scripts/v22_text_flow_patch.py"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: portfolio-v2.2-text-flow-stress
      - name: Apply patch
        run: python3 scripts/v22_text_flow_patch.py
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add de/index.html en/index.html ru/index.html css/v2.css
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Fix: rebalance multilingual text flow"
          git push origin HEAD:portfolio-v2.2-text-flow-stress
